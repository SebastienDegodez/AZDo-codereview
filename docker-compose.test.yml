services:
  microcks:
    image: quay.io/microcks/microcks-uber:latest
    ports:
      - "8585:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=uber
    volumes:
      - ./tests/mocks:/artifacts
    command: >
      --spring.profiles.active=uber
      --artifacts.directory=/artifacts
